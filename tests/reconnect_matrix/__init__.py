"""The reconnect scenario bed: every history from the change's catalogue, staged and probed.

A CHECKLIST FIRST, A TEST SUITE SECOND. Scenarios that cannot be staged yet — because the feature
they need does not exist — are declared and reported as BLOCKED with the reason, so the list is
complete before the implementation is. See
`openspec/changes/reconnect-sync-controller/scenarios-network-events.md`.
"""
