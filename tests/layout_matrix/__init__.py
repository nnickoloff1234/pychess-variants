"""A layout survey for the bughouse pages: every popular viewport, every page state.

Not a test. It produces an HTML report and never fails a run — see `viewports.py` for the two
declared lists, `driver.py` for the walk and `report.py` for the output.
"""
